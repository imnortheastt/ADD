"""engine_pin — single-source ENGINE_MD5 pin.

One constant, one home. The five prose-only suites import this value instead
of each carrying a duplicate hard-coded literal. When the engine legitimately
changes, re-aim this one line and the entire tooling suite re-anchors.

The pin is a hard-coded literal — never computed at runtime. A pin that
recomputes its own value from the file it is supposed to guard is vacuous:
it can never detect drift. The literal was recorded at the commit that first
introduced it and is updated only by a deliberate, human-approved task.
"""

ENGINE_MD5 = "8a8967a151710f35dd9d3dea3ca86566"  # re-aimed @ next-footer-engine (every COMPLETING exit-0 mutating verb now prints ONE engine-sourced `next:` footer, next-step-seams task 1/3: a single resolver _next_footer(root, state) — Arm A names the active in-flight task's phase command (advance, or the gate verbs at verify, guarded gate=="none" AND phase!="done" so a HARD-STOPped task falls to Arm B not back to re-gate) + its PHASE_GUIDE why; Arm B reuses _decide_next_base over the active-milestone rollup — the SAME precedence the report renders — with the empty-rows branch reshaped command-first ("decompose into tasks — add.py new-task <ms>", replacing the dead-end "none — no tasks yet"). Wired into advance·gate·new-task·new-milestone·phase·use·reopen·set-milestone·milestone-done·lock·stage·archive-milestone·compact, converging their ad-hoc tails (new-task "then: advance", new-milestone "Decompose…", gate HARD-STOP "return to BUILD", milestone-done "Confirm…") so each prints exactly ONE next-step. EXCEPTIONS (human-confirmed at tests, both keep their existing seam, no footer): the exit-3 HEAL paths (cmd_heal · gate→heal raise SystemExit(3) with a return_to_build redo message) and cmd_init (its bootstrap "next: run /add"/"lock" block already names the setup-flow next step; the generic resolver would regress it). Fail-soft: the footer is computed AFTER save_state and never crashes a saved mutation — no active milestone / unreadable doc / corrupt rollup degrade to "next: add.py status — re-orient". Pure render, writes nothing; the trailing marker slot is reserved for the sibling gate-owner-marker. read-only surfaces (status/guide/report) unchanged. _scope_guard no longer dies in place on the RECOVERABLE findings — an out-of-scope touch (source 'scope') and a present-but-wrong sidecar (diverged|unparseable, source 'scope-tamper') call the SAME _heal_or_escalate the tamper tripwire uses, returning the task to BUILD for an honest redo (exit 3) and counting against the ONE shared per-task HEAL_CAP; the (CAP+1)th confirmed finding records gate=HARD-STOP and escalates heal_exhausted. The ERASED baselines stay die-in-place (exit 1, no heal): a MISSING sidecar is scope_snapshot_tampered and an erased anchor under a present sidecar is scope_anchor_missing — a redo cannot recreate erased evidence (tripwire_missing parity). Every heal reason CARRIES its named code so the refusal-token assertions still match; placement unchanged — after _tamper_guard, BEFORE the waiver write, off the HARD-STOP path; a heal never rewrites the sidecar or anchor. check stays a read-only WARN monitor — never routes to heal, never increments. This pin supersedes re-aimed @ scope-violation-heal (whose recoverable-heal mechanics it preserves verbatim, described above). Carries the prior scope-gate-enforce gate (NEW _declared_scope parses the frozen §5 grammar — backticked tokens · FIRST 'Scope (may touch):' line · ./… task dir · '/' project root · bare sibling-of-previous · outside-root dropped · directory token = WHOLE subtree · no line = UNDECLARED grandfathered · garbage = [] no cover; tests->build _scope_walk {rel: md5} pruning _SCOPE_EXCLUDE_DIRS into sidecar scope-snapshot.json anchored by sidecar-md5; v2 anchor×sidecar co-witness after the anchor-erase REFUTE; v3 UNDECLARED-crossing cleanup), the engine-merge-base-enforcement wave-ledger gate (cmd_check wave section + read-only cmd_wave_verify, v4 status-field grammar, exactly-one echo column, six drift fixtures), and the engine-argv-portability re-bind. tool-agnostic — never runs git)
